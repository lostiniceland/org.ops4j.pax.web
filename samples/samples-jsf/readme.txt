Note - these samples are not real JSF/Primefaces samples.
These are libraries/modules that use the structure and files required for a Web application with JSF, but only
for the purpose of checking if correct resources can be loaded from complex Web Application Bundles (WABs).
